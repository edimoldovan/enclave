[package]
name = "enclave-ui"
version = "0.1.0"
edition = "2024"
description = "Shared look and feel for the Enclave apps: theme, fonts, keymap, ribbon kit, icons"

[dependencies]
eframe = "0.33.3"
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.4"
